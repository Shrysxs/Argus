// @argus/shared-types — Cross-package TypeScript types for Argus (DecisionPayload, AgentVote, DecisionRecord, WalletSession, LogFilter, etc.)

// TODO: Define the core type interfaces described in AGENTS.md §4.1 (ChainAdapter types)
// and §5.2 (structured agent output schema: vote, confidence, reasoning, key data points cited).
export {};
